---
- name: Set up training VM
  hosts: local

  roles:
    - role: initial_setup
      tags: initial_setup
      when: initial_setup

    - role: docker
      tags: docker
      when: docker

    - role: catrust
      tags: catrust
      when: catrust

    - role: common #  <- mandatory
      tags: common

    - role: epics-modules
      tags: epics_modules
      when: epics_modules

    - role: epics-tools
      tags: epics_tools
      when: epics_tools

    - role: bluesky
      tags: bluesky
      when: bluesky
      become: true
      become_user: "{{ dev_user }}"

  environment: "{{ proxy_env }}"
